import { IComponent } from "./index";

function SubstanceAsideMenuModel(props: IComponent) {
	const { head, titleBtn, anyBtn } = props;

	return { head, titleBtn, anyBtn };
}

export default SubstanceAsideMenuModel;
