import { Styles } from "Styles/Styles";
import { CSSObject } from "@emotion/react";

class HistoryPage_PublicStyles extends Styles {
	public wrapper: CSSObject = {
		height: 300,
		width: 500,
	};
}

export default new HistoryPage_PublicStyles();
